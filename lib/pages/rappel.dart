import 'package:flutter/material.dart';

class Rappel extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(
      title: const Text('Rappel'),
      centerTitle: true,
      backgroundColor: Colors.red,
    ),
  );
}
